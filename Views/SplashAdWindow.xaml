<Window x:Class="BnetSwitch.SplashAdWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ctrl="clr-namespace:BnetSwitch.Controls"
        Title="推广" WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
        AllowsTransparency="True" Background="Transparent" WindowStartupLocation="CenterOwner"
        SizeToContent="Height" Width="352" FontFamily="Microsoft YaHei UI" FontSize="13">
    <Border Background="{DynamicResource Win}" CornerRadius="12" Margin="16"
            BorderBrush="{DynamicResource Bd}" BorderThickness="1" ClipToBounds="True">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.4" Color="#000000" />
        </Border.Effect>
        <StackPanel>
            <!-- 广告素材区 -->
            <Grid Height="176" Background="{DynamicResource AccSoft}" Cursor="Hand" MouseLeftButtonUp="OnAdClick">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ctrl:IconView Geometry="{StaticResource GeoPhoto}" Stroke="{DynamicResource Tx3}" Thickness="2" IconSize="30" HorizontalAlignment="Center" />
                    <TextBlock x:Name="AdTextBlock" Foreground="{DynamicResource Tx3}" FontSize="12.5" TextWrapping="Wrap"
                               TextAlignment="Center" Margin="24,8,24,0" />
                </StackPanel>
                <Image x:Name="AdImage" Stretch="UniformToFill" />
                <Border Background="#8C141A28" CornerRadius="4" Padding="6,1" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="8">
                    <TextBlock Text="广告" Foreground="White" FontSize="10" />
                </Border>
                <Border Background="#8C141A28" CornerRadius="14" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="8"
                        Cursor="Hand" MouseLeftButtonUp="OnClose">
                    <StackPanel Orientation="Horizontal" Margin="9,3">
                        <ctrl:IconView Geometry="{StaticResource GeoX}" Stroke="White" Thickness="2" IconSize="12" Margin="0,0,4,0" />
                        <TextBlock Text="关闭" Foreground="White" FontSize="11.5" />
                    </StackPanel>
                </Border>
            </Grid>
            <!-- 底注 -->
            <DockPanel Margin="12,9">
                <TextBlock DockPanel.Dock="Left" Text="推广内容 · 与本工具功能无关" Foreground="{DynamicResource Tx3}" FontSize="11.5" VerticalAlignment="Center" />
                <TextBlock DockPanel.Dock="Right" HorizontalAlignment="Right" VerticalAlignment="Center" Cursor="Hand" MouseLeftButtonUp="OnRemoveAds">
                    <Run Text="永久去广告 →" Foreground="{DynamicResource Acc}" FontSize="11.5" />
                </TextBlock>
            </DockPanel>
        </StackPanel>
    </Border>
</Window>
